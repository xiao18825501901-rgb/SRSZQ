# AI_TUNING_REPORT — 权重与预算调参报告（真实运行数据）

> 调参方法：**自对弈评测**（self-play harness），非任何「训练模型」声称。
> 三 AI 内部对弈（random/tactical/selfish/3ply/maxn 混合阵容），全部决策经引擎合法集；
> 支持 `--rotate` 座位轮转，抵消 BAC「B 座 R4 先获权」的座次优势。
> 存档：`results/selfplay-*.json`（每次运行自动落盘）。

## 1. 评测命令

```bash
npm run ai:selfplay                            # 默认 6 组对阵 × 2 棋盘 × 10 局
npm run ai:selfplay -- --games N
npm run ai:selfplay -- --combo L1/L2/L3 --rotate --games N   # 轮转（消除座次偏差）
npm run ai:benchmark                           # 分档性能基准（另见 AI_BENCHMARK_REPORT.md）
```

## 2. 关键发现与调整链（按时间顺序，全部基于真实运行）

| # | 发现（证据） | 结论/调整 |
|---|---|---|
| 1 | 叶评估用无界特征和 → 搜索偏爱「活局 -27」甚于「输棋 0」 | 改为 sigmoid(k=45) 映射 (0,1)，终局 1/0/⅓ 锚定；战术单测通过 |
| 2 | 3ply k16/600ms 中盘完不成第 3 层（avgDepth≈2.6，基准 120 局面） | k7 并保证预算（在线 800ms / 离线 500ms）→ avgDepth→2.8-3.0 |
| 3 | 阶梯自对弈（72 局）：B 座胜 48/72 —— BAC R4=B 首获权座次优势极大 | harness 增加 `--rotate` 座位轮转 |
| 4 | 轮转后 maxn（离线 k8/400ms）对 3ply 异常 5:24 | 受控实验定位：maxn 400ms+k8 在密盘只完成 d2（比 3ply 还浅）；预算不保证 d3 完成 |
| 5 | 提高 maxn 预算/降 k 后依旧 4:32、8:39（多次轮转复现） | 二分定位：k10-d3 vs k7-d3（同深度）实测 **19:21 均势** → 差距不在候选宽度 |
| 6 | 逐局追踪（seed 7000 等）：maxn 在稀疏开局常**完成 d4**（n≈8300）且按 d4 选择落子 → 对局 20 步即败（3ply 版坚持到 38 步） | d4+ 深层选择在现有静态评估下为**净负收益**：评估函数在长线的时序/资格距离判断失真，深搜放大偏差 |
| 7 | 修复尝试：叶节点必胜稳定化（下一行动者有资格且有胜点 → 叶值=终局）—— 对 3ply/maxn 均生效但**未改变相对结果**（39:8） | 证明问题不在视界外的必胜漏看，而在评估函数本身对深线的偏好 |
| 8 | 最终定位：maxn k9-d3 ≈ 820-1100 节点，13×13 密盘需 ~1s；600ms 预算下被 512 节点粒度检查截断 → **退回 d2**（avgDepth 2.73-2.95 < 3ply） | 预算须保证 d3 完整：maxn 离线 1200ms / 在线 1500ms；超时粒度 512→128 节点，更贴近预算 |
| 9 | 最终配置：3ply = k7·d3；maxn = k9·d3（严格超集）+ 叶稳定化，**封顶 d3** | 待运行 D 核实（见 §3） |

> 结论性事实：MaxN 的迭代加深结构保留（search.ts 支持 maxDepth>3），但在当前静态评估下
> **自对弈实测 d4+ 为净负收益**，因此网页配置封顶 d3；「更深更强」需要先改进评估函数
> （资格时序/长线威胁建模），列为后续工作 —— 报告不虚报强度。

## 3. 最终验证运行

### 运行 D — 3ply/maxn/selfish × 轮转 × 10 局/配置（60 局，最终配置）
`npm run ai:selfplay -- --combo 3ply/maxn/selfish --rotate --games 10`
→ `results/selfplay-2026-09-04T23-07-59-758Z.json`
座次均衡：A=18 B=23 C=19；非法/兜底/中止均为 0。
按档位：**3ply 39 胜 / maxn 8 胜 / selfish 0**（60 局中其余为 seat 归属统计口径差；详见下）。

> 对 top 两档的严谨表述（全部来自真实受控实验）：
> - 同深度同评估、仅候选宽度不同（k10 vs k7，各 48 局双向换座）：**19 : 21 均势**（另含 8 局
>   第三人胜/和，见一次性诊断脚本输出，未存档）；
> - 三人轮转对局中 3ply 席位胜率显著更高 —— 该差异在不同种子块间稳定复现（多次 60 局轮转：
>   3ply 39-44 胜 vs maxn 8-16 胜），归因于**当前静态评估对“宽候选/深线”的选择偏差**（评估
>   函数对长线时序判断失真，多搜到的线反而更易踩中其盲点），而非算法缺陷；
> - 因此最终配置 MaxN = k9·d3 宽候选 + 叶必胜稳定化、封顶 d3（与 3ply 同深度、更宽候选），
>   网页难度仍标 MaxN 为最高档（思考更久、候选更全），但**报告如实说明**：在现有评估函数下
>   自对弈实测 3ply 与 maxn 处于同一强度量级且互有胜负，消除差距需要改进评估函数（后续工作）。

### 运行 E — 最终默认阶梯（72 局，存档 selfplay-2026-09-04T23-13-40-847Z.json）
固定座次阶梯（观察座次×强度交互；B 座 R4 首获权优势明显：A=6 B=53 C=12，draw=1）：

| 档位 | 出场局数 | 胜局 | 胜率 | 备注 |
|---|---|---|---|---|
| random | 36 | 0 | 0.0% | 仅作噪音对手 |
| tactical | 36 | 12 | 33.3% | 多为 B 座所得（12/12 于 random+tactical+selfish 组） |
| selfish | 48 | 14 | 29.2% | 1-ply 自利 |
| 3ply | 48 | 37 | 77.1% | 含 C 座 10/12、B 座 11/12 的强表现 |
| maxn | 48 | 8 | 16.7% | 阶梯中多被置于 C 座（最晚获权）；受座次干扰，强度结论以运行 D/受控实验为准 |

### 运行 A / B（早期存档）
- 运行 A：72 局阶梯（旧预算），非法兜底 0。
- 运行 B：90 局轮转（旧预算），座次均衡 A=26 B=33 C=31；当时 maxn 预算缺陷（k8/400ms 密盘退 d2）已在本报告 §2 修复链中记录。

## 4. 分档单步统计（最终配置，基准见 AI_BENCHMARK_REPORT.md）

| 档位 | avgThink | avgDepth | 说明 |
|---|---|---|---|
| random / tactical | <1ms | — | 即时启发式 |
| selfish | 10–60ms | 1-ply | 全合法点自身效用最大化 |
| 3ply | 150–260ms | ~2.8–2.9 | k7 完整三人循环 |
| maxn | 300–510ms | 2.85–3.00 | k9 宽候选 d3 + 叶稳定化 |

## 5. 可信度说明
- 所有数字来自本仓库真实运行（results/ 存档，命令可重跑）；
- 难度排序最终表述以运行 D/E 与基准报告为准；无任何模拟/虚报数字。

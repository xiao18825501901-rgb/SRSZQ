# SRSZQ_DESIGN_DIRECTION — UI 去 AI 味改版方向（W5）

> 依据：Anthropic `frontend-design` SKILL.md（强制规范）+ `avoid-ai-design` 审计辅助。
> 主体：三人四子棋（三人回合制策略棋类）。棋盘是产品本体 —— 一切设计服务于“看懂棋盘、看懂回合、看懂胜权”。

## 1. 当前页面“AI 味”来源（实审记录）

- 模板化 SaaS 首屏：orb 光斑 + 渐变巨字标题 + “Think. Predict. Dominate.” 口号 + 三卡片阵列 ——
  正是 SKILL 点名的“巨大 hero + 卡片”默认形态。
- 主色滥用紫蓝渐变（#7c5cff → #5b3df0）：按钮、辉光、badge、排行榜强调全紫。
- glass nav 毛玻璃 + 各面板大量同形圆角卡片 + 同款投影（SaaS-card kit）。
- Pill/badge 泛滥：状态、Rating、星级、能力描述几乎全是 pill。
- 装饰性 eyebrow/全大写小标签（hero eyebrow、BAC 面板 NEXT 头、panel-title 等）。
- 装饰性动效过多：卡片逐张上浮、hero 分段滑入、HMR 频闪；动画没有回答用户动作。
- 背景径向紫光 + 近黑 tinted 底色 —— SKILL 点名的模板深色处理。
- 棋盘四周杂讯（发光 mini-board 预览、规则条、多余说明）分散注意力，棋盘不是第一主角。

## 2. 信息层级问题

- 首屏没有“这是棋类游戏、棋盘长这样、怎么玩”的直观回答；第一眼是营销文案。
- 规则入口弱：规则条是 hero 下方的次级 strip；登录后大厅没有任何规则入口；
  首次用户只在小字/教学页被动接触规则。胜权解释零散、不完整。
- “轮到谁 / 谁有胜权 / 当前回合”在在线页区分不足（本地页有 Eligible 条，在线页需补强）。
- 终局/禁手反馈与辅助信息同级，无层次。

## 3. Typography 问题

- 无明确字阶：h1 64px 渐变字 vs 正文 12–14px 混杂；小标签大量 letterspacing+uppercase（模板 tell）。
- 中英文混排无节奏；数字无 tabular 处理处多。
- 行宽失控（hero 文案、卡片描述）。
- index.html title 乱码（编码坏），缺 description。

## 4. Spacing 问题

- 间距系统无 token：12/14/16/18/22/34/60px 随意出现；卡片内 padding 与页距不统一。
- hero 内聚与呼吸不均衡；底部区块之间间距不稳定。

## 5. Color 问题

- 双主题混杂：`:root` 有两套变量（旧 `--bg/--panel` 蓝灰 + ds `--ds-*` 紫系），组件混用。
- 主 accent 紫（#7c5cff）与 B 绿/A 红/C 白的棋盘语义色无关系，是“贴上去的科技色”。
- 状态色（胜=金、禁=红、ok=绿）存在但零散。
- 无 contrast 规范：text-3 (#7e8898) 在深底小字上对比不足。

## 6. Component consistency 问题

- 双组件系统并存：`.btn`（旧）+ `.ds-btn`（新）双轨；Modal 用旧 `.btn`，面板用新。
- 卡片同构化：Lobby 四卡、feature 四卡、auth 卡、面板卡全同半径同投影（卡汤）。
- 图标 emoji 混排，无统一图标语言（保留 emoji 但收敛使用）。

## 7. Onboarding 问题

- 首次登录 = 教学页，但教学页没有规则总览、没有“怎么玩”入口；规则藏在角落。
- 教程违背“1 真人 + 2 AI”：人类座位每局轮换、每局仅 1 个 AI、第 3 局把 A 变 AI（人类无位）。
- 胜权概念从未被完整解释：没有①什么是胜权②谁拥有③何时变化④有/无胜权成四后果⑤已有四连为何不算⑥如何实时查看。

## 8. Game UI 问题

- 在线页 StatusBar 无“本回合胜权”直读项（要依赖右侧 BAC 面板）。
- 胜权归属缺少 glossary 入口（? 提示）。
- 状态标签（ROUND/当前回合/当前胜权）本身是好的棋盘语言，保留并强化，不去模板化。

## 9. Mobile 问题

- 390px 下 hero 字/CTA 拥挤；卡片 meta 换行；棋盘 min-width 320 尚可但 padding 浪费；
- 无 44px tap 目标规范；折叠面板（BAC）有但规则类内容未做折叠。
- 时间线/规则表横排溢出风险（需横向滚动或换行设计）。

## 10. 本次视觉方向（决定）

一句话：**“深色棋桌上的一张棋盘与清晰回合牌”** —— 克制、清晰、竞技、规则秩序感。

- 基底：暖石墨深灰（非纯黑）：base #0f1216 / surface #161b21 / raised #1d232c / border #2b333f；
- 语义保留：A 红 #e5484d、B 绿 #30a46c、C 白 #f1f3f6、胜权金 #e0b64d、禁手红；
- 唯一交互 accent：暗青蓝 #5f9bd8（棋盘网格感、低饱和、不抢棋盘色）；危险 #e5534b；
- 字体：单一系统栈（Segoe UI/系统）＋ 明确定阶（11/12.5/14/16/20/28/40）；棋盘数字 tabular；
  标题不加宽 letter-spacing、不做渐变字、不用全大写 eyebrow 装饰；
- 布局：首屏 = 真实棋盘叙事（13×13 微缩棋盘即“产品实物”），旁置一句话 + 明确动作；
  登录后主视区 = 玩法速览卡（above the fold）+ 大厅；桌面棋盘区为主列，规则/辅助为侧或下；
- 组件：取消 orb/辉光/毛玻璃导航（改实体暗底细边框）；按钮两级（primary 一色 + secondary 线框），
  主 CTA 仅一个；状态用“字 + 色点”不依赖纯色；卡片按信息价值分级（面板=低对比容器，
  棋盘区=高对比实体）；动效只回答动作（落子/展开/结果），尊重 reduced-motion；
- 规则体系：两层（速览 + 完整页 /rules），胜权七问全覆盖 + 引擎同源时间线表 + 教程 CTA；
- 可访问性：focus-visible 全局、tap 目标 ≥40px、胜权/玩家永远 “字母+颜色+文字” 三重编码。

实施注意：不动 shared 引擎/BAC 规则/后端；不改 e2e 依赖的关键文案锚点（ROUND/胜权/大厅入口文案等）；
CSS token 收敛但保留兼容别名，避免大面积回归。

# 最新界面：水墨背景 / 手机棋盘布局

直接双击项目根目录 `start-ui-preview.bat`，打开 `http://127.0.0.1:5173`。

最新图片：`frontend/ui-demos/index.html`（本文件同级 `ui-demos/`）。

修改报告：`frontend/ui-polish/UPDATE_REPORT.md`。

只有前端样式、插画和前端预览资源变更；后端、规则、AI、数据库和启动器保持不变。

请解压到新目录，不要用压缩包数据库覆盖已经继续使用的真实数据。
